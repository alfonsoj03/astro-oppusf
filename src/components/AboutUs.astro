---
import teamwork from "../assets/teamwork.png";
import Breadcrumb from "./Breadcrumb.astro";

interface CoreValue {
    number: number;
    title: string;
    description: string;
}

const coreValues: CoreValue[] = [
    {
        number: 1,
        title: "Innovación constante",
        description:
            "Siempre vamos un paso adelante, aplicando tecnología de vanguardia que supere las expectativas de nuestros clientes y eleve la eficiencia de tus soluciones de iluminación.",
    },
    {
        number: 2,
        title: "Calidad sin compromisos",
        description:
            "Cada luminaria que entregamos pasa por estrictos controles y pruebas acreditadas, buscamos ofrecerte productos confiables y consistentes.",
    },
    {
        number: 3,
        title: "Disponibilidad inmediata",
        description:
            "Sabemos como funciona el sector, por eso trabajamos para mantener inventario permanente y procesos ágiles que reducen demoras y aceleran tu avance.",
    },
    {
        number: 4,
        title: "Cercanía con el cliente",
        description:
            "Tu éxito es nuestro éxito. Primero comprendemos tus necesidades reales para adaptar soluciones que realmente funcionen en tu contexto.",
    },
    {
        number: 5,
        title: "Transparencia total",
        description:
            "Comunicación clara, procesos honestos y precios competitivos en el mercado para que puedas tomar decisiones con confianza.",
    },
    {
        number: 6,
        title: "Cumplimiento y respaldo normativo",
        description:
            "Nuestros productos cumplen normas oficiales mexicanas y estándares internacionales, incluyendo certificaciones para áreas peligrosas.",
    },
    {
        number: 7,
        title: "Compromiso a largo plazo",
        description:
            "No termina en la compra ni instalación. Te brindamos soporte y garantías extendidas para que tu inversión esté segura a lo largo del tiempo.",
    },
];

const breadcrumbItems = [{ label: "Inicio", href: "/" }, { label: "Nosotros" }];
---

<section class="pt-32 pb-12 bg-white text-zinc-950">
    <div class="container mx-auto px-6">
        <div class="max-w-6xl mx-auto">
            {/* Breadcrumb */}
            <div class="mb-8">
                <Breadcrumb items={breadcrumbItems} />
            </div>

            <!-- Header Section -->
            <div class="text-center mb-16 animate-on-scroll" data-delay="0">
                <h2
                    class="text-5xl lg:text-6xl lg:text-7xl mb-8 leading-tight font-bold"
                >
                    Lo que nos hace <span class="italic text-[#FF8351]"
                        >diferentes</span
                    >
                </h2>
                <div
                    class="max-w-3xl mx-auto space-y-6 text-lg lg:text-xl text-zinc-700 leading-relaxed"
                >
                    <p>
                        En <b>Opplux</b> no solo creamos soluciones de iluminación
                        profesional: <b>somos tu aliado estratégico</b> para proyectos
                        que no pueden esperar.
                    </p>
                    <p>
                        Nuestra visión se basa en mantener un amplio stock
                        permanente para brindarte <b>disponibilidad inmediata</b
                        >, y en ofrecer un compromiso contigo a través de <b
                            >respaldo técnico y garantías confiables</b
                        >, para asegurarnos de que tu proyecto se lleve a cabo a
                        tiempo, de forma segura y con tranquilidad operativa.
                    </p>
                    <p>
                        Nuestra experiencia forma parte del ecosistema <b
                            >Oppus Global LLC</b
                        >, donde nuestro equipo técnico pone en marcha
                        soluciones de ingeniería para parques solares,
                        impulsando así la eficiencia operativa y la
                        confiabilidad de las instalaciones, asegurando
                        resultados sostenibles y de alto desempeño.
                    </p>
                </div>
            </div>

            <!-- Image Section -->
            <div
                class="relative rounded-3xl overflow-hidden shadow-2xl animate-on-scroll"
                data-delay="200"
            >
                <img
                    src={teamwork.src}
                    alt="Equipo Opplux"
                    class="w-full h-[400px] md:h-[500px] lg:h-[600px] object-cover"
                />
                <!-- Optional overlay for better contrast -->
                <div
                    class="absolute inset-0 bg-gradient-to-t from-black/10 to-transparent pointer-events-none"
                >
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Values Section with Animated Background -->
<section class="py-32 bg-zinc-950 text-white relative overflow-hidden">
    <!-- Animated background gradient -->
    <div class="background-gradient absolute inset-0 opacity-30"></div>

    <!-- Subtle floating orbs -->
    <div
        class="floating-orb-1 absolute top-1/4 left-1/4 w-96 h-96 bg-[#FF8351]/5 rounded-full blur-3xl"
    >
    </div>
    <div
        class="floating-orb-2 absolute bottom-1/4 right-1/4 w-96 h-96 bg-[#FF8351]/5 rounded-full blur-3xl"
    >
    </div>

    <div class="container mx-auto px-6 relative z-10">
        <div class="max-w-6xl mx-auto">
            <!-- Values Header -->
            <div class="mb-20 animate-on-scroll" data-delay="0">
                <p
                    class="text-zinc-400 tracking-[0.2em] uppercase text-sm mb-6"
                >
                    NUESTROS VALORES
                </p>
                <h2
                    class="text-4xl md:text-5xl lg:text-6xl leading-tight font-bold"
                >
                    Lo que más importa para nosotros.
                </h2>
            </div>

            <!-- Values Grid -->
            <div
                class="grid grid-cols-1 md:grid-cols-2 lg:gap-x-16 gap-x-16 md:gap-x-8 gap-y-12"
            >
                {
                    coreValues.map((value, index) => {
                        const colors = [
                            "#3b82f6", // blue
                            "#10b981", // green
                            "#f59e0b", // amber
                            "#ef4444", // red
                            "#8b5cf6", // purple
                            "#ec4899", // pink
                            "#14b8a6", // teal
                        ];
                        const color = colors[index % colors.length];
                        return (
                            <div
                                class="flex gap-6 animate-on-scroll"
                                data-delay={index * 100}
                            >
                                {/* Number Circle */}
                                <div class="flex-shrink-0">
                                    <div
                                        class="w-12 h-12 rounded-full border-2 flex items-center justify-center font-bold"
                                        style={`border-color: ${color}; color: ${color};`}
                                    >
                                        {value.number}
                                    </div>
                                </div>

                                {/* Content */}
                                <div class="flex-1">
                                    <h3 class="text-xl lg:text-2xl mb-3 text-white font-bold">
                                        {value.title}
                                    </h3>
                                    <p class="text-zinc-400 md:text-[14px] lg:text-[16px] leading-relaxed">
                                        {value.description}
                                    </p>
                                </div>
                            </div>
                        );
                    })
                }
            </div>
        </div>
    </div>
</section>

<style>
    /* Scroll-triggered animations */
    .animate-on-scroll {
        opacity: 0;
        transform: translateY(30px);
        transition:
            opacity 0.6s ease-out,
            transform 0.6s ease-out;
    }

    .animate-on-scroll.visible {
        opacity: 1;
        transform: translateY(0);
    }

    /* Background gradient animation */
    @keyframes gradient-shift {
        0% {
            background: radial-gradient(
                circle at 20% 50%,
                rgba(161, 161, 170, 0.08) 0%,
                transparent 50%
            );
        }
        25% {
            background: radial-gradient(
                circle at 80% 50%,
                rgba(161, 161, 170, 0.08) 0%,
                transparent 50%
            );
        }
        50% {
            background: radial-gradient(
                circle at 50% 80%,
                rgba(161, 161, 170, 0.08) 0%,
                transparent 50%
            );
        }
        75% {
            background: radial-gradient(
                circle at 50% 20%,
                rgba(161, 161, 170, 0.08) 0%,
                transparent 50%
            );
        }
        100% {
            background: radial-gradient(
                circle at 20% 50%,
                rgba(161, 161, 170, 0.08) 0%,
                transparent 50%
            );
        }
    }

    .background-gradient {
        animation: gradient-shift 20s linear infinite;
    }

    /* Floating orbs animation */
    @keyframes float-1 {
        0%,
        100% {
            transform: translate(0, 0) scale(1);
        }
        50% {
            transform: translate(100px, -50px) scale(1.2);
        }
    }

    @keyframes float-2 {
        0%,
        100% {
            transform: translate(0, 0) scale(1);
        }
        50% {
            transform: translate(-100px, 50px) scale(1.3);
        }
    }

    .floating-orb-1 {
        animation: float-1 15s ease-in-out infinite;
    }

    .floating-orb-2 {
        animation: float-2 18s ease-in-out infinite;
    }
</style>

<script>
    // Intersection Observer for scroll animations
    const observerOptions = {
        threshold: 0.1,
        rootMargin: "0px 0px -100px 0px",
    };

    const observer = new IntersectionObserver((entries) => {
        entries.forEach((entry) => {
            if (entry.isIntersecting) {
                const delay =
                    parseInt(entry.target.getAttribute("data-delay") || "0") ||
                    0;
                setTimeout(() => {
                    entry.target.classList.add("visible");
                }, delay);
                observer.unobserve(entry.target);
            }
        });
    }, observerOptions);

    // Observe all elements with animation class
    document.querySelectorAll(".animate-on-scroll").forEach((el) => {
        observer.observe(el);
    });
</script>
