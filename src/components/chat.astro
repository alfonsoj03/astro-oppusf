    {/* Mobile Header */}
    <div class="container mx-auto px-6 flex md:hidden items-center justify-between relative z-[60]">
        {/* Logo */}
        <a href="/" class="flex items-center gap-2 relative z-[60]">
            <img
                src={oppluxLogo.src}
                alt="Opplux"
                id="mobile-logo"
                class="h-10 w-auto object-contain transition-all duration-300"
            />
        </a>

        <div class="flex items-center gap-4">
            {/* Mobile Cart Icon (Visible only when menu is closed) */}
            <a
                href="/cotizacion"
                id="mobile-cart-btn"
                class:list={[
                    "relative p-2 transition-opacity duration-300",
                    isBlackTheme ? "text-white" : "text-white",
                ]}
            >
                <ShoppingCart class="w-6 h-6" />
                {/* Mobile Cart counter badge */}
                <span id="mobile-cart-counter" class="absolute top-0 right-0 bg-[#FF8351] text-white text-[10px] font-bold rounded-full w-4 h-4 flex items-center justify-center transition-transform duration-300 hidden">
                    0
                </span>
            </a>

            {/* Mobile Menu Toggle (Animated Hamburger) */}
            <button
                id="mobile-menu-btn"
                class="md:hidden p-2 relative z-[70] focus:outline-none"
                aria-label="Menu"
            >
                <div class="w-6 h-5 flex flex-col justify-between items-center">
                    <span class:list={["w-full h-0.5 bg-current transform transition-all duration-300", isBlackTheme ? "bg-white" : "bg-white"]} id="line-1"></span>
                    <span class:list={["w-full h-0.5 bg-current transition-all duration-300", isBlackTheme ? "bg-white" : "bg-white"]} id="line-2"></span>
                    <span class:list={["w-full h-0.5 bg-current transform transition-all duration-300", isBlackTheme ? "bg-white" : "bg-white"]} id="line-3"></span>
                </div>
            </button>
        </div>
    </div>

} else {
                // Open Menu
                mobileMenu?.classList.remove("translate-x-full");
                mobileMenu?.classList.add("translate-x-0");

                const container = line1?.parentElement;
                container?.classList.add("scale-75");
                
                // Animate hamburger to X
                // Line 1: Baja 9px al centro y rota 45 grados
                line1?.classList.add("rotate-45", "translate-y-[9px]");
                // Line 2: Desaparece por completo
                line2?.classList.add("opacity-0");
                // Line 3: Sube 9px al centro y rota -45 grados
                line3?.classList.add("-rotate-45", "-translate-y-[9px]");
                
                // Force lines to black for visibility on white menu
                [line1, line2, line3].forEach(line => {
                    if (line) {
                        line.classList.remove("bg-white");
                        line.classList.add("bg-black");
                    }
                });

                // Hide mobile cart btn (replaced by link inside menu)
                if (mobileCartBtn) mobileCartBtn.classList.add("opacity-0", "pointer-events-none");

                // Disable body scroll
                document.body.style.overflow = 'hidden';
            }