---
import { Settings, Package, ClipboardCheck } from "lucide-astro";

const features = [
    {
        icon: Settings,
        title: "Programación y personalización real",
        description:
            "Adaptamos cada modelo a tus requerimientos exactos. Ajusta precisión y distribución lumínica sin sacrificar disponibilidad.",
    },
    {
        icon: Package,
        title: "Stock planeado y directo con fábrica",
        description:
            "Inventario listo para entrega inmediata. Minimizamos tiempos y aseguramos tu continuidad operativa.",
    },
    {
        icon: ClipboardCheck,
        title: "Productos probados en laboratorios acreditados ante la EMA",
        description:
            "Equipos probados en laboratorios acreditados. Evidencia técnica y respaldo para las auditorías más exigentes.",
    },
];
---

<section class="py-24 md:py-12 lg:py-24 bg-black text-white">
    <div class="container mx-auto px-6">
        {/* Header Section */}
        <div
            class="flex flex-col md:flex-row justify-between items-start gap-12 mb-20"
        >
            <div class="max-w-2xl md:max-w-md lg:max-w-2xl">
                <p
                    class="text-[#FF8351] tracking-[0.2em] uppercase text-sm mb-4"
                >
                    SOLUCIONES ESCALABLES
                </p>
                <h2
                    class="md:text-4xl text-7xl lg:text-7xl font-semibold tracking-tighter leading-[0.9]"
                >
                    Operación contínua, sin fricción
                </h2>
            </div>
            <div class="max-w-md md:max-w-sm lg:max-w-md">
                <p
                    class="text-zinc-400 md:text-md lg:text-lg text-lg leading-relaxed"
                >
                    En Opplux ponemos a disposición nuestra experiencia para
                    ofrecer soluciones a <b>precios competitivos</b> que te acompañan
                    en el crecimiento de tu operación. Evita tener fricción o sorpresas:
                </p>
            </div>
        </div>

        {/* Features Grid */}
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8 md:gap-12">
            {
                features.map((feature, index) => {
                    const Icon = feature.icon;
                    return (
                        <div
                            class="feature-item group opacity-0 translate-y-8 transition-all duration-700 ease-out"
                            style={`transition-delay: ${index * 150}ms`}
                        >
                            {/* Icon Container */}
                            <div class="mb-6 w-16 h-16 flex items-center justify-center border-2 border-zinc-800 rounded-lg group-hover:border-[#FF8351] transition-colors duration-300">
                                <Icon class="w-8 h-8 text-zinc-400 group-hover:text-[#FF8351] transition-colors duration-300" />
                            </div>

                            {/* Title */}
                            <h3 class="text-lg lg:text-xl mb-4 group-hover:text-[#FF8351] transition-colors duration-300">
                                {feature.title}
                            </h3>

                            {/* Description */}
                            <p class="text-zinc-400 text-smleading-relaxed">
                                {feature.description}
                            </p>
                        </div>
                    );
                })
            }
        </div>
    </div>
</section>

<script>
    const observerOptions = {
        root: null,
        rootMargin: "0px",
        threshold: 0.1,
    };

    const observer = new IntersectionObserver((entries) => {
        entries.forEach((entry) => {
            if (entry.isIntersecting) {
                entry.target.classList.remove("opacity-0", "translate-y-8");
                observer.unobserve(entry.target);
            }
        });
    }, observerOptions);

    document.querySelectorAll(".feature-item").forEach((item) => {
        observer.observe(item);
    });
</script>
